export default theme => ({
    container: {
        backgroundColor: theme.palette.other.darkBlue,
    },
    wrapper: {
      color: theme.palette.common.white,
    },
    textLoop: {
        opacity: 0.6,
      },
  });